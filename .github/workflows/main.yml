name: Test

on:
  push:
    branches: [main]
  pull_request: [main]
    branches: [main]
    
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
      - uses: actions/checkout@v2
      
      - name: Run a one-line scripts
        run: echo Hello, world!
